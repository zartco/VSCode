## 2024-06-24 - Visible Triggers for Global Shortcuts
**Learning:** Global keyboard shortcuts (like Ctrl+K) must always be paired with a persistently visible, accessible UI element that both triggers the action and advertises the shortcut key to ensure discoverability.
**Action:** Added a visible "Search" button to the sidebar with a "Ctrl+K" hint that manages the shared SearchPalette state.

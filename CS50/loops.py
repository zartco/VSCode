# range(5) generates a sequence: 0, 1, 2, 3, 4
# The loop runs once for each number in that sequence
# Each time through, the variable 'i' holds the current number

for i in range(9):
    print(i)   # this line is indented — that's the "body" of the loop
